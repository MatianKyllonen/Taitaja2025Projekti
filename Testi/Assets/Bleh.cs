using System;
using UnityEngine;

public class Bleh : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("jgdskgjdskgj");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
